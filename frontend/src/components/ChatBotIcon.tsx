import { AiOutlineRobot } from "react-icons/ai";

export const ChatBotIcon = () => {
  return (
    <AiOutlineRobot
      size={40}
      style={{
        backgroundColor: "skyblue",
        padding: 10,
        borderRadius: "50%",
      }}
    />
  );
};
